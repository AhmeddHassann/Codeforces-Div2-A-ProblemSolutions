#include<bits/stdc++.h>
using namespace std;
int main()
{
	string s;
	cin >> s;
	sort(s.begin(), s.end());
	int cnt = 0;
	for (int i = s.length() - 1; i >= 0; i--)
		if (s[i] == '7' || s[i] == '4')
			cnt++;
 
	if (cnt == 4 || cnt == 7)
		cout << "YES";
	else
		cout << "NO";
 
}
