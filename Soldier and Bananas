#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long int k, n, w;
	cin >> k >> n >> w;
	long long int need;
	need = k * (w * (w + 1) / 2);
	if (need <= n)
		cout << 0;
	else
		cout << need - n;
}
